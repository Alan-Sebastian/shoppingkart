<section class="orders">
    <div class="container p-3"> 
        <div class="row p-2">
<table class="table mt-5  p-3 pl-3">
<tbody>
{{#each orders}}
    <tr> 
      <td>{{this.price}}</td> 
    <td>{{this.deliveryDetails.address}},{{this.deliveryDetails.pincode}}</td> 
      <td>{{this.deliveryDetails.mobile}}</td>     
      <td>
        <a href="/view-order/{{this._id}}" class="btn btn-success ml-auto">View Product</a>
      </td>
   </tr>  
    {{/each}}
  </tbody>
</table>
 </div> 
     </div>
</section>